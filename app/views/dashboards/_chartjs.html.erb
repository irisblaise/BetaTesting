<canvas
  id="myChart"
  width="400"
  height="400"
  data-labels="<%= @versions.map{|version| version.name} %>"
  data-data= "<%= @versions.map{|version| version.calculate_avg_score } %>"
></canvas>
